{
	"author": "Paul Williams",
	"name": "p17",
	"description": "Svelte library experimenting with form behaviour and user interaction (layout and style uses UK GDS as a guide)",
	"type": "module",
	"license": "MIT",
	"version": "0.1.2",
	"engines": {
		"node": ">=18"
	},
	"repository": {
		"type": "git",
		"url": "https://github.com/PaulioRandall/p17.git"
	},
	"keywords": [
		"svelte",
		"forms",
		"plunder"
	],
	"exports": {
		"./package.json": "./package.json",
		".": {
			"svelte": "./index.js"
		}
	},
	"files": [
		"./index.js"
	],
	"scripts": {
		"fmt": "prettier --plugin prettier-plugin-svelte --log-level warn --write .",
		"build": "vite build",
		"dev": "npm run fmt && vite dev --port 3000",
		"commit": "npm run fmt && npm run build && cat ./scripts/ready-to-ship.txt"
	},
	"devDependencies": {
		"@sveltejs/adapter-auto": "3.2.0",
		"@sveltejs/kit": "2.5.5",
		"prettier": "3.2.5",
		"prettier-plugin-svelte": "3.2.2",
		"vite": "5.2.8"
	},
	"peerDependencies": {
		"svelte": "4.x"
	}
}
